{
  "name": "agent-email",
  "type": "module",
  "description": "Gmail client for the agent inbox — OAuth2 (default) or App Password/IMAP",
  "dependencies": {
    "imapflow": "^1.0.0",
    "nodemailer": "^6.9.0"
  }
}
